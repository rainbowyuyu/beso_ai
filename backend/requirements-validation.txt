# Optional validation module deps (install alongside backend/requirements.txt):
#   pip install -r backend/requirements-validation.txt
matplotlib>=3.8
pypdf>=4.0
PyYAML>=6.0
python-docx>=1.1.0
# Surrogate inference (optional; only when use_surrogate=true and a model is trained):
#   pip install -r backend/requirements-surrogate.txt
joblib>=1.3
scikit-learn>=1.3
