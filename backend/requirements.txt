# 安装到项目 .venv（推荐）:
#   .\.venv\Scripts\python -m pip install -r backend\requirements.txt
# 旧示例 .venv_web 同理。
# Pydantic v2 required for FastAPI 0.115 (do not mix with pydantic 1.x globally).
#
# Pin set checked with pip install --dry-run.

fastapi==0.115.14
uvicorn[standard]==0.32.1
pydantic==2.10.6
websockets==14.2
python-multipart==0.0.20
python-dotenv==1.0.1
requests==2.32.3
numpy==2.3.4
networkx==3.6.1
meshio==5.3.5
gmsh==4.15.2
cadquery-ocp==7.9.3.1
python-docx>=1.1.0
# pythonocc-core：Windows 上 PyPI 常无匹配轮子，会导致整份 requirements 安装失败。
# 梁解析 OCC 路径为可选；OC4 设计域 Gmsh 链路不依赖此项。需要时再装：
#   pip install -r backend/requirements-occ.txt
#
# text-to-cad / cad_skill_step（build123d STEP 生成，可选）：
#   pip install -r backend/requirements-text-to-cad.txt
